"""The static page at adaptive.peterparker.ca: its data files and a server that sends the
headers the live site sends.

The page itself is `demo/`, five files a stranger can read. This package writes the JSON it
reads, so that every number on it comes out of the same artefacts the README's table does and
nothing on the page is typed by hand.
"""
